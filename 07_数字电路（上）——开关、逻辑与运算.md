# 第七章：数字电路（上）——开关、逻辑与运算

## 本章定位

自第5章以来，晶体管一直在做一件事：工作在饱和区，部分导通地**放大**信号。但晶体管还有另一种完全不同的活法——完全导通/完全截止，做**开关**。

这一转，电子世界从”处理连续量”跨入了”处理 0 和 1”——数字电路诞生。

**为什么需要这个转变？** 从信号角度看，模拟处理的是”灰度”——信号在每个瞬间的真值，优点是保留了全部信息，缺点是小扰动就破坏信息（噪声混进去分不清）。数字处理的是”黑白”——信号被二值化成了 0 和 1。二值化丢弃了灰度细节，但换来了一个巨大的好处：**抗干扰。** 噪声要把一个 0 误认成 1，需要大到跨越整个电源电压——这是模拟信号想都不敢想的容错能力。序言里”三个参数一条命”中的信噪比——在数字域，这个问题被重新定义了。

本章讲数字电路的前半段：晶体管怎么当开关、怎么拼出逻辑门、怎么用逻辑门搭出能算算术的组合逻辑。

---

## 1. 开关——同一个管子，另一种人生

同一个 MOSFET，栅极电压把它推入两种截然不同的工作状态：

- **模拟用法**（第5-6章）：Vgs 略高于 Vth → 饱和区 → Ids 随 Vgs 连续变化 → 处理的是“黑到白之间所有的灰度”
- **数字用法**（本章）：Vgs >> Vth → 完全导通，Vds ≈ 0（线性区）；Vgs < Vth → 完全关断，Id ≈ 0（截止区）。只有两个状态——“通”和“断”

### 为什么数字能赢

**抗干扰。** 0 和 1 之间隔着整个电源电压——噪声要让系统误解一个 0 为 1，需要大到跨越整个电源轨。而模拟信号是连续量，噪声稍大一点就蒙对了蒙错了——无法区分。

代价是失去了“连续性”——你不能用一根数字线直接传温度（除非先用 ADC 把温度变成数字编码，那是第 6 章的事了）。

---

## 2. CMOS 反相器——数字世界的最小单元

把一个 PMOS 和一个 NMOS 串在一起：PMOS 在上（源极接 VDD，漏极接输出），NMOS 在下（源极接 GND，漏极接输出），两个栅极连在一起做输入。

- 输入 = 0：NMOS 关断（Vgs = 0 < Vth），PMOS 导通（Vgs = -VDD < Vth_p）。PMOS 把输出拉到 VDD → **输出 = 1**
- 输入 = 1：NMOS 导通（Vgs = VDD > Vth），PMOS 关断（Vgs = 0 > Vth_p）。NMOS 把输出拉到 GND → **输出 = 0**

**关键设计特征：任何时候只有一个管子导通**——从 VDD 到 GND 之间没有直流通路（除了极短暂的开关瞬间，两个管子同时微微导通的那一刹那）。无数个这样的门拼在一起，静态功耗趋近于零。

这就是 CMOS 改变世界的原因。1963 年，仙童的 Frank Wanlass 在 ISSCC 上提交了一篇四页纸的短文，提出了 CMOS 逻辑电路。当时的论文几乎没人注意——但它解决的恰恰是序言第五幕里那个关键瓶颈：**用双极型逻辑门搭大型数字系统，功耗会大到不可接受。** Wanlass 的发明让每个逻辑门只在翻转那一刹那耗电——静态功耗趋近于零。从此，数字集成电路的规模不再被功耗锁死。每一代工艺缩小尺寸，电容变小 → 充放电更快 → 速度更快、功耗更低、密度更高——摩尔定律在 CMOS 工艺上跑了半个多世纪。

---

## 3. 逻辑门——用开关拼出“逻辑运算”

从反相器出发，加一个输入就得到了实际芯片里最常用的两种基本门：

- **与非门（NAND）**：两个 NMOS 串联（只有两个输入都 = 1，两个管子都通，输出才能拉到 0），两个 PMOS 并联（只要有一个输入 = 0，就有一个 PMOS 通，输出就拉到 1）
- **或非门（NOR）**：NMOS 并联（任一输入 = 1 就拉到 0），PMOS 串联（两个输入都 = 0 才都导通拉到 1）

NAND 和 NOR 最常用，因为 CMOS 实现最简洁。从它们出发，用布尔代数组合，可以拼出任何逻辑函数。

**布尔代数的核心**：与（AND）、或（OR）、非（NOT）三种基本运算，加上**德摩根定律**——“与的非 = 非的或”、“或的非 = 非的与”——可以在 NAND 和 NOR 之间自由转换。

---

## 4. 组合逻辑——当场算

逻辑门直接连接，输出纯由当前输入决定——这叫**组合逻辑**。没有记忆、没有状态、没有时间概念——输入一变输出就变（经过门延迟）。

### 基本积木

- **加法器**：
  - 半加器：两个 1 位数相加 → 和 + 进位。不能处理从低位来的进位。
  - 全加器：三个 1 位数相加（A + B + 低位进位）→ 和 + 进位。N 个全加器串联 = N 位行波进位加法器——简单但慢（进位从最低位一路“涟漪”到最高位）。
  - 超前进位加法器：用额外逻辑“预判”进位 ⇒ 更快的加法，代价是更多逻辑门。
- **译码器 / 编码器**：N 选 1（比如 3 线 → 8 线，输入 3 位二进制，激活 8 路输出中对应的那一路）
- **多路选择器（MUX）**：数据层面的选通开关——从多路输入中选一路输出，由选择信号控制。MUX 是 FPGA 和数字数据通路的底层积木。

### 组合逻辑的致命缺陷

**没有记忆。** 可以用来算（加法器），但不能“存住算的结果”。不能存储中间结果、不能“等下一步”、不能根据“之前发生过什么”来改变行为。下一步需要的东西——记忆——是第 8 章的主题。

---

## 本章在整本书中的位置

- ← 第4章（同一个 MOSFET，本章只用它的"开关"那一面）、第5章（放大和开关是同一个 gm 的两面——模拟看连续，数字看通断）
- ← 第6章（ADC 把模拟信号翻译成 0 和 1 之后，本章的逻辑门负责处理这些 0 和 1）
- → 第8章：组合逻辑会算但不会记——下一步，给数字电路加上"记忆"，让它能存住状态、按时间顺序做事。从状态机到 CPU 的最后一步
